# DLProjectMRE
B.Sc. Deep Learning course project: a multimodal system that uses a fine-tuned ResNet-50 CNN to identify food from an input image and passes the top 3 predicted classes to a LLaMA 2 7B model to generate an appropriate recipe.
